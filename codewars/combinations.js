function combinations2 (arr) {
  let results = [];
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      
    }
  }
}

console.log(combinations2(['a', 'b', 'c', 'd']));